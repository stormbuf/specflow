# 后端错误处理规范

定义错误类型、错误传播、日志记录与 API 错误响应的统一约定。

## Overview

<!-- 引导问题：回答以下问题后再填写具体章节 -->

<!--
- 定义了哪些错误类型？是否有统一的错误基类或错误码体系？
- 错误如何传播？是抛异常、返回 Result/Either，还是 error 作为返回值最后一个参数？
- 错误如何记录？哪些错误需要记录，记录到哪个级别？
- 错误如何返回给客户端？API 错误响应的统一格式是什么？
- 是否区分"业务错误"（可预期）与"系统错误"（不可预期）？
- 错误信息是否对客户端脱敏？堆栈是否暴露？
- 错误码体系如何设计？是 HTTP status 映射还是自定义业务码？
-->

（由团队填写：概述错误处理的核心原则与错误分类体系。）

## 错误类型

（由团队填写：定义项目中使用的错误类型体系。包括：

- 错误基类 / 接口定义
- 业务错误（预期错误，如"余额不足"）
- 系统错误（非预期错误，如 DB 连接失败）
- 校验错误（入参不合法）
- 每种错误的构造方式与携带字段
）

## 错误处理模式

（由团队填写：定义错误传播与处理的统一模式。包括：

- 错误在哪里被捕获与转换（middleware / handler / service）
- 是否允许"吞掉"错误（catch 后不处理）
- 错误包装（wrap）的约定，保留原始 cause
- panic / throw 的使用边界
）

## API 错误响应

（由团队填写：定义 API 错误响应的统一格式。例如：

```json
{
  "code": "BUSINESS_ERROR_CODE",
  "message": "面向用户的可读信息",
  "details": {}
}
```

包括：HTTP status 与业务错误的映射规则、错误码命名规则、是否返回 trace_id。）

## 常见错误

（由团队填写：列出团队在错误处理上犯过的错误，例如：

- 把系统错误的堆栈直接返回给客户端，泄露内部信息
- catch 了错误却不记录，问题无法排查
- 用字符串 message 区分错误类型，导致判断脆弱
- 错误码散落各处，没有统一注册，出现重复
- 业务错误与系统错误混在一起处理，导致 500 频发
）

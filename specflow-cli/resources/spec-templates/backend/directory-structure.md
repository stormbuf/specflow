# 后端目录结构规范

定义后端代码的模块划分、目录布局与命名约定，确保新增文件有明确归属。

## Overview

<!-- 引导问题：回答以下问题后再填写具体章节 -->

<!--
- 模块如何组织？是按业务领域（feature/domain）划分，还是按技术层（controller/service/repository）划分？
- 业务逻辑放在哪一层？是否有独立的 domain/service 层，还是散落在 controller 里？
- API 端点定义在哪？是否有统一的路由注册入口，还是分散在各模块？
- 工具函数如何组织？是否有共享的 utils 包，还是按模块内聚？
- 配置文件、常量、类型定义分别放在哪？
- 跨模块共享代码放在哪？如何避免循环依赖？
-->

（由团队填写：概述当前后端项目的目录组织哲学与核心原则。）

## 目录布局

（由团队填写：用代码块展示实际目录树，标注每个顶层目录的职责。例如：

```
src/
├── modules/          # 业务模块，按领域划分
├── shared/           # 跨模块共享代码
├── config/           # 配置加载
├── infra/            # 基础设施（DB、缓存、消息队列）
└── app.ts            # 应用入口
```
）

## 模块组织

（由团队填写：单个业务模块内部如何组织。说明各文件的职责边界，例如 handler → service → repository 的调用链。）

## 命名约定

（由团队填写：文件名、目录名、导出符号的命名规则。例如：

- 文件名：kebab-case / camelCase / snake_case
- 目录名：单数 / 复数
- 导出：默认导出还是命名导出
- 测试文件命名：`xxx.test.ts` / `xxx.spec.ts`
）

## 示例

（由团队填写：以一个真实业务模块为例，展示完整的目录结构与文件内容骨架。）

## 常见错误

（由团队填写：列出团队在目录组织上犯过的错误，例如：

- 把所有 controller 堆在一个目录里，导致目录爆炸
- service 层直接操作数据库，绕过 repository
- 跨模块直接 import 内部实现，而非通过公开接口
- 测试文件与源文件分离过远，难以对应
）

# 前端类型安全规范

定义类型模式、验证策略与运行时类型检查约定。

## Overview

<!-- 引导问题：回答以下问题后再填写具体章节 -->

<!--
- 类型模式是什么？interface vs type 的使用边界？
- 验证策略？API 响应是否做运行时校验？用什么库（zod / yup / io-ts / valibot）？
- 运行时类型检查？哪些边界需要？如何与编译期类型联动？
- 泛型的使用规范？何时该用、何时过度？
- 联合类型与可辨识联合（discriminated union）的使用？
- 类型推导与显式标注的边界？
- 第三方库的类型补充（@types）管理？
-->

（由团队填写：概述类型安全的核心原则与分层策略。）

## 类型模式

（由团队填写：定义类型定义的约定。包括：

- `interface` 与 `type` 的使用边界
- props 类型必须显式定义并导出
- 联合类型与字面量类型的使用场景
- 泛型的使用规范与上限
- 工具类型（`Pick` / `Omit` / `Partial` / `Record`）的使用
- 类型导出与组织方式
）

## 验证策略

（由团队填写：定义数据验证的策略。包括：

- API 响应是否做运行时校验
- 选用的校验库及原因
- schema 与类型的联动方式（如 zod 的 `z.infer`）
- 表单校验与提交校验的分层
- 校验失败的处理方式
- 环境变量与配置的校验
）

## 运行时检查

（由团队填写：定义运行时类型检查的边界。包括：

- 哪些边界必须做运行时检查（API 入口、外部输入、localStorage 读取）
- 哪些场景可以信任编译期类型
- 类型守卫（type guard）的编写规范
- `unknown` 与 `any` 的使用边界
- 断言（`as`）的使用限制与替代方案
）

## 常见错误

（由团队填写：列出团队在类型安全上犯过的错误，例如：

- 用 `as` 强制断言绕过类型检查，运行时数据结构与预期不符导致崩溃
- 信任 API 返回结构不做校验，后端改字段前端静默出错
- 泛型过度抽象，类型定义比实现还复杂
- 用 `any` 代替 `unknown`，丢失了 narrowing 能力
- 类型与 schema 分别维护，两边不同步
- 第三方库缺类型就写 `any`，不查 `@types`
）

# 前端状态管理规范

定义本地状态、全局状态与服务器状态的管理边界与实现方式。

## Overview

<!-- 引导问题：回答以下问题后再填写具体章节 -->

<!--
- 本地状态用什么？useState / useReducer？何时用哪个？
- 全局状态用什么？Context / Zustand / Redux / Pinia / Jotai？
- 服务器状态如何管理？是否与全局状态分离？用 React Query / SWR？
- 状态放在哪一层？何时提升、何时下沉？
- 派生状态（computed）如何处理？
- 状态持久化策略？localStorage / sessionStorage / cookie？
- 状态更新的不可变性与规范？
-->

（由团队填写：概述状态管理的整体策略与分层原则。）

## 本地状态

（由团队填写：定义组件内部状态的管理约定。包括：

- useState 的使用场景
- useReducer 的使用场景（复杂状态机）
- 状态提升的时机与判断标准
- 受控组件与非受控组件的选择
- 本地状态的初始化约定
）

## 全局状态

（由团队填写：定义全局状态的管理方式。包括：

- 选用的状态管理库及原因
- 哪些状态该放全局（用户信息、主题、权限等）
- store 的组织方式（单 store 还是多 store / slice）
- 状态更新方式（action / 直接修改 / immer）
- 全局状态与组件的订阅粒度，避免过度渲染
）

## 服务器状态

（由团队填写：定义服务器状态的管理方式。包括：

- 是否与全局状态分离管理
- 选用的库（React Query / SWR / 自封装）
- query key 的命名规则
- 缓存与失效策略
- mutation 后的刷新策略
- 乐观更新的使用边界
）

## 常见错误

（由团队填写：列出团队在状态管理上犯过的错误，例如：

- 把服务器数据塞进全局状态手动管理，导致缓存与失效逻辑混乱
- 状态放得过高，导致无关组件全量重渲染
- 用 useEffect + useState 模拟 derived state，而非直接计算
- 直接修改状态对象，破坏不可变性
- 全局状态爆炸，什么都往里塞
- 多个组件各自维护同一份服务器数据的副本，导致不一致
）

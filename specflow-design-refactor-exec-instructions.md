# SpecFlow Design 阶段重构执行指令

> 本文档是给 LLM 执行重构的完整指令。按顺序执行以下 5 个步骤。每步给出精确的文件路径、修改位置和修改内容。
>
> 项目根目录：`/Users/stormbuf/project/specflow/`

---

## 执行概览

共需修改 5 个文件：

| 序号 | 文件 | 修改类型 |
|------|------|---------|
| 1 | `specflow-workflow/references/design.md` | **重写**（设计阶段定义） |
| 2 | `specflow-workflow/assets/design.md` | **重写**（设计模板） |
| 3 | `specflow-workflow/assets/tasks-template.md` | **定点修改**（拆解依据章节号 + covers 引用） |
| 4 | `specflow-workflow/references/tasks.md` | **定点修改**（拆解规则第5步章节号） |
| 5 | `specflow-workflow/SKILL.md` | **定点修改**（审查范围表） |

所有路径相对于 `/Users/stormbuf/project/specflow/`。

---

## 步骤 1：重写 `references/design.md`

将整个文件替换为新内容。

关键变更：
- 目标：从"8个设计维度"改为"公共骨架 + 族插槽"
- 输入：增加 proposal 变更类型作为族选择依据
- 设计维度：重构为 §0 公共前置 + 功能族§1-§8（微调）/ 优化族§1-§5（新增）/ 迁移族§1-§6（新增）+ §9 公共收尾
- 讨论循环：从"8个维度"改为"§0 + 族插槽 + §9"
- 避坑约束：去掉"至少5个错误码"，增加"§9不得因族类型跳过"
- 审查：从"八维覆盖"改为"各族维度覆盖"
- 完成条件：去掉"至少5个错误码"，改为分族覆盖

新文件内容见同目录下 `design-refactored.md`（如存在）或直接按下方规范编写。

### 新 design.md 完整结构规范

文件开头保持：

```
# Design Stage

本文件定义技术实现设计阶段。Design 说明如何在既有 System Architecture / ADR 约束下实现 proposal 和 spec-delta，不重新定义用户需求，也不临场决定系统架构图或长期 ADR。

路径锚定：本文件中的 `specflow/`、源码、测试、配置和部署路径均相对于 `{PROJECT_ROOT}/`；不得在 `{SKILL_DIR}/` 下查找 proposal、spec-delta、design 或长期项目文档。
```

#### 目标（改）

- 从已确认的 proposal 和 spec-delta 转入技术实现设计，回答"怎么做"
- 根据变更类型选择对应族插槽，系统性地覆盖该族的所有设计维度
- 发现本次实现是否需要新增或调整系统边界图、系统架构图或 ADR
- 所有 UML 使用 Mermaid 表达
- 避免只画图而忽略数据设计和异常处理

#### 输入（改）

- proposal.md — **必读**，提取变更类型（functional / nonfunctional / infrastructure / lightweight）
- spec-delta.md — 功能族必需
- 相关主 spec、architecture.md、adr/、源码、测试、配置和部署事实

#### 输出（不变）

- design.md

#### 族选择规则（新增）

```
读取 proposal.md 的变更类型字段：
  functional    → 功能族插槽（§1-§8）
  nonfunctional → 优化族插槽（§1-§5）
  infrastructure → 迁移族插槽（§1-§6）
  lightweight   → 不进入 Design 阶段
```

#### 设计维度与质量标准（重写）

##### §0. 变更上下文（公共前置，所有族共享）

必须覆盖：变更类型（从proposal继承）、上游约束（从architecture/ADR提取）、影响面摘要（改动涉及哪些模块、接口、数据、配置）。

##### 功能族插槽（§1-§8）

**§1 架构与模块定位** — 模块划分、分层职责、核心设计模式（如适用）。原内容基本保留，"核心设计模式"改为"如适用"。

**§2 核心流程 UML** — 原内容保留，"至少用一张图"改为"如果核心流程涉及≥2个组件协作或有分支判断，必须用至少一张图；流程简单时文字+表格即可"。

**§3 数据设计** — 原内容保留。3a 数据模型6子项加注"均需填写，如果某子项确实不适用写'不适用'并说明理由"。3b 缓存设计改为"本功能主动读写缓存时适用"。3c 消息/事件设计改为"本功能主动生产或消费异步消息时适用"。

**§4 接口设计** — 原内容保留。4a 错误码清单从"至少覆盖：参数校验失败、资源不存在、权限不足、服务不可用、未知错误"改为"覆盖该接口所有可预见的失败模式"。4a 增加接口版本兼容策略（如适用）。4b 名称从"内部接口"改为"模块间交互契约"。4c 不变。

**§5 关键业务规则与实现逻辑** — 原名称"关键算法 / 业务规则实现"改为"关键业务规则与实现逻辑"。原内容保留。

**§6 异常处理与边界条件** — 原内容保留。异常场景列表增加"对应错误码（见§4a）"列。幂等性设计增加"不需要幂等的操作写'不需要'并说明理由"。

**§7 非功能性设计** — 原内容保留。性能改为"量级预估（10/100/1000/10000+ QPS）"。限流/降级/批处理参数改为"如适用，不适用时写'本功能不涉及'"。

**§8 测试与可验证性设计** — 原名称"测试与可验证性建议"改为"测试与可验证性设计"。正常场景改为"覆盖 spec-delta 中的所有 Gherkin Scenario"。

##### 优化族插槽（§1-§5）— 全新

**§1 优化目标与基线** — 1a 优化目标（从proposal继承）、1b 当前基线指标（值/测量方法/测量条件）、1c 目标指标（目标值/达标判据）

**§2 优化方案设计** — 2a 方案对比（预期提升/风险/复杂度/副作用）、2b 推荐方案与理由、2c 影响面分析

**§3 实现设计** — 3a 核心改动、3b 数据变更（如适用）

**§4 风险与回滚** — 4a 优化引入的新风险、4b 行为回归风险、4c 回滚方案

**§5 验证策略** — 5a 行为回归测试、5b 指标对比测试、5c 边界与极端场景

##### 迁移族插槽（§1-§6）— 全新

**§1 迁移目标与范围** — 1a 迁移目标（从proposal继承）、1b 迁移范围（组件/当前状态/目标状态/迁移类型）、1c 迁移约束（停机时间/兼容性/数据量/时间窗口）

**§2 迁移流程设计** — 2a 迁移阶段（操作/耗时估计/可回滚/验证方法）、2b 迁移流程图（Mermaid flowchart）、2c 切换策略（蓝绿/滚动/影子/停机）

**§3 数据迁移设计** — 3a 数据转换规则（源结构/目标结构/转换逻辑/数据量）、3b 数据一致性校验（方法/工具/时机/不一致处理）、3c 数据回滚方案

**§4 兼容性设计** — 4a 新旧版本并存、4b 接口兼容性（如适用）、4c 配置兼容性（如适用）

**§5 异常处理与回滚** — 5a 迁移失败场景（触发条件/处理策略/回滚步骤）、5b 回滚计划（触发条件/步骤/时间估计/验证）、5c 部分迁移失败处理

**§6 验证策略** — 6a 迁移完成验证、6b 行为回归测试、6c 性能基线对比

##### §9. System Architecture / ADR 影响（公共收尾，所有族共享）

保持公共收尾定位，编号固定为§9。增加分族架构影响检查清单：

- 功能族典型触发点：新增对外接口/用户角色/外部系统/独立部署模块/技术栈/认证方案
- 优化族典型触发点：替换中间件/调整模块边界/改变存储策略/引入可观测性方案/改变数据所有权
- 迁移族典型触发点：替换数据库框架运行时/改变部署架构/改变CI基础设施/改变配置管理方式/迁移期间系统边界图暂时性变化

#### 系统架构 / ADR 前置（不变）

#### 讨论循环（改）

- 读取 proposal、spec-delta、architecture、ADR、源码和配置
- 填写 §0 变更上下文
- 根据 proposal 变更类型选择族插槽
- 遍历上游规约，记录待覆盖清单（功能族：spec-delta场景和关键约束；优化族：proposal优化目标；迁移族：proposal迁移范围）
- 按选中族插槽维度顺序逐项展开
- 检查所有维度完整性（分族检查重点）
- 验证全覆盖（分族验证逻辑）
- 汇总架构与ADR影响：FOR EACH 当前族所有维度的讨论结果（不再是"§1-§8"）
- 写盘 §0 + 族插槽 + §9 + 开放问题

#### 开放问题（改）

来源表从"8维度"改为分族列出：
- 功能族：§1-§8 各维度可能的开放问题场景
- 优化族：§1/§2/§4 可能的开放问题场景
- 迁移族：§1/§2/§3/§4/§5 可能的开放问题场景

#### 避坑约束（改）

1. 禁止只画UML而忽略数据设计和异常处理（功能族适用）
2. 时序图必须真实反映调用层次（功能族适用）
3. 接口设计必须包含完整错误码清单（功能族适用）— 从"至少5个"改为"覆盖所有可预见失败模式"
4. 数据设计不得因存储形式特殊而跳过或弱化（功能族适用）
5. 讨论循环不可跳过任何维度（所有族适用）
6. §5不得以"无关键业务规则"为由跳过而未引用spec-delta原文佐证（功能族适用）
7. **新增**：§9架构影响检查不得因族类型而跳过（所有族适用）

#### 阻断（微调）

"design与spec-delta描述的行为冲突"改为"功能族：design与spec-delta描述的行为冲突"

#### 审查（改）

从"八维覆盖"改为分族审查：
- 所有族通用：族插槽选择一致性、§0完整性、§9架构影响一致性、§9分族检查清单逐项检查
- 功能族专项：spec-delta行为一致性、八维覆盖、数据设计完整性、接口错误码覆盖、§6与§4交叉引用
- 优化族专项：§1基线可测量、§2推荐理由、§4回滚可行、§5验证策略覆盖
- 迁移族专项：§2阶段完整可回滚、§3校验方法明确、§4兼容性可行、§5回滚计划完整

#### 完成条件（改）

从"八维+5个错误码"改为分族覆盖：
- 所有族：§0完整、族插槽选择一致、architecture.md存在、§9逐项检查
- 功能族：模块分层明确、UML覆盖、数据模型/缓存/消息/接口/错误码/第三方策略明确、关键算法/异常/边界/幂等明确、性能/安全/监控/配置/测试明确、接口错误码覆盖所有失败模式、§6与§4交叉引用
- 优化族：§1基线和目标可测量、§2推荐理由、§3可落地、§4回滚可行、§5覆盖行为回归和指标对比
- 迁移族：§1范围和约束明确、§2阶段完整可回滚、§3转换/校验/回滚完整、§4兼容性可行、§5回滚和部分失败完整、§6覆盖迁移验证/回归/基线对比

---

## 步骤 2：重写 `assets/design.md`（设计模板）

将整个文件替换为新模板。

新模板结构：
1. 标题 + 使用说明
2. §0 变更上下文（公共前置）
3. 功能族插槽（§1-§8）— 含引导式表格和 Mermaid 模板
4. 优化族插槽（§1-§5）— 含引导式表格
5. 迁移族插槽（§1-§6）— 含引导式表格
6. §9 System Architecture / ADR 影响（公共收尾，含分族检查清单注释）
7. 开放问题

模板使用 `<!-- -->` 注释作为引导提示，填充时保留结构删除不需要的注释。三个族插槽用 HTML 注释分隔，使用时保留对应族插槽、删除其他两个。

完整模板内容见步骤 1 中各维度的必须覆盖项，转换为填空表格形式。

---

## 步骤 3：定点修改 `assets/tasks-template.md`

### 修改 3a：上游产物依赖表（第 15-17 行）

| 原文 | 改为 |
|------|------|
| `design §1（模块）+ §4（接口）` | `design §1（模块定位）+ §4（接口设计）` |
| `design §7（非功能优化点）` | `design §2（优化方案设计）的各优化点` |
| `design §3（数据迁移）+ §6（异常）` | `design §2（迁移流程设计）的各阶段 + §3（数据迁移设计）的各迁移点` |

### 修改 3b：优化族子项说明（第 91-92 行）

| 原文 | 改为 |
|------|------|
| `design §7（非功能优化点清单）` | `design §2（优化方案设计）的各优化点` |
| `design §7 优化点` | `design §2 优化点` |

### 修改 3c：优化族基线测量步骤（第 97 行）

| 原文 | 改为 |
|------|------|
| `covers: design §7.<优化点>` | `covers: design §1b.<优化点>` |

### 修改 3d：迁移族子项说明（第 115-116 行）

| 原文 | 改为 |
|------|------|
| `design §3（数据迁移）+ §6（异常处理）` | `design §2（迁移流程设计）的各阶段 + §3（数据迁移设计）的各迁移点` |
| `design §3/§6 迁移点` | `design §2/§3 迁移点` |

### 修改 3e：迁移族迁移单元步骤（第 121 行）

| 原文 | 改为 |
|------|------|
| `covers: design §3/§6.<迁移点>` | `covers: design §2/§3.<迁移点>` |

---

## 步骤 4：定点修改 `references/tasks.md`

### 修改 4a：拆解规则第 5 步

| 原文 | 改为 |
|------|------|
| `优化族：design §7（非功能优化点清单）` | `优化族：design §2（优化方案设计）的各优化点` |
| `迁移族：design §3（数据迁移）+ §6（异常处理）` | `迁移族：design §2（迁移流程设计）的各阶段 + §3（数据迁移设计）的各迁移点` |

### 修改 4b：拆解规则第 7 步

| 原文 | 改为 |
|------|------|
| `优化族：标注 design §7 优化点` | `优化族：标注 design §2 优化点` |
| `迁移族：标注 design §3/§6 迁移点` | `迁移族：标注 design §2/§3 迁移点` |

---

## 步骤 5：定点修改 `SKILL.md`

### 修改 5a：审查范围表（第 175 行）

| 原文 | 改为 |
|------|------|
| `与 spec-delta 行为一致性、八维覆盖完整性、模块分层清晰、数据设计完整、错误码不少于 5 个` | `族插槽选择与 proposal 变更类型一致、各族维度覆盖完整性、§0 上下文完整、§9 架构影响与 architecture.md / ADR 一致、§9 分族检查清单逐项检查` |

---

## 验证清单

执行完以上 5 个步骤后，检查以下各项：

- [ ] `references/design.md` 中不再出现"8 个设计维度"或"八维"字样（已改为"族插槽"）
- [ ] `references/design.md` 中不再出现"至少 5 个错误码"或"错误码不少于 5 个"字样
- [ ] `references/design.md` 中 §9 包含分族架构影响检查清单
- [ ] `references/design.md` 中避坑约束有 7 条（原 6 条 + 新增第 7 条）
- [ ] `references/design.md` 中讨论循环的 §9 汇总逻辑为"当前族所有维度"而非"§1-§8"
- [ ] `assets/design.md` 包含 §0 + 功能族（§1-§8）+ 优化族（§1-§5）+ 迁移族（§1-§6）+ §9 + 开放问题
- [ ] `assets/design.md` 功能族 §6 异常场景列表含"对应错误码（见 §4a）"列
- [ ] `assets/design.md` 功能族 §8 名称为"测试与可验证性设计"（无"建议"二字）
- [ ] `assets/tasks-template.md` 中优化族拆解依据引用 design §2，迁移族引用 design §2+§3
- [ ] `references/tasks.md` 中拆解规则第 5 步和第 7 步章节号已更新
- [ ] `SKILL.md` 审查范围表已更新，不再出现"八维"或"错误码不少于 5 个"
